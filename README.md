## ecomm-test-app

A tiny Streamlit demo that mimics a simple e-commerce flow:

- Fake login
- Browse products and add to cart
- Place an order (shipping + contact fields)
- Checkout confirmation (no real payments)

### Run

```bash
streamlit run main.py
```

Open the URL Streamlit prints (usually `http://localhost:8501`).
